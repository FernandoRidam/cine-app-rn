import {
  StyleSheet,
} from 'react-native';

const Styles = StyleSheet.create({
  separator: {
    width: '100%',
    height: .4,
    marginTop: 15,
    marginBottom: 5,
    backgroundColor: 'rgba( 255, 255, 255, .4 )',
  }
});

export default Styles;
