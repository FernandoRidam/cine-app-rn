import {
  StyleSheet,
} from 'react-native';

const Styles = StyleSheet.create({
  button: {
    alignItems: 'center',
    justifyContent: 'center',
    width: '90%',
    height: 45,
    borderWidth: 1,
    borderColor: '#FFF',
    borderRadius: 8,
  },

  selected: {
    borderColor: '#FFC527',
    color: '#FFC527',
  },

  textButton: {
    fontSize: 14,
    color: '#FFF',
  },
});

export default Styles;
